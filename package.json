{
  "name": "edacy_files_walk",
  "version": "1.0.0",
  "description": "Module to get all files path in a directory",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Tabara",
  "license": "ISC",
  "dependencies": {
    "fs": "^0.0.1-security"
  }
}
